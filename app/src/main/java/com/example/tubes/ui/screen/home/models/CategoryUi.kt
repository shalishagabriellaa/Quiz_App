package com.example.tubes.ui.screen.home.models

data class CategoryUi(
    val id: String,      // ini akan kita isi pakai categoryId (bukan docId)
    val name: String,
    val bannerUrl: String?
)
